# Vite environment variables (copy to .env.local for local development)
# Backend base URL (FastAPI)
VITE_API_BASE_URL=http://0.0.0.0:8015

# Auth integration
VITE_UMS_LOGIN_URL=https://rent-managment-system-user-magt.onrender.com/
VITE_CLIENT_ID=YOUR_NEW_CLIENT_ID
VITE_AUTH_CALLBACK_PATH=/auth/callback
# Optionally override redirect base (defaults to window.location.origin)
# VITE_REDIRECT_BASE=http://localhost:5173

# Set to 'true' to bypass auth locally (not recommended when connecting to real backend)
VITE_BYPASS_AUTH=false
